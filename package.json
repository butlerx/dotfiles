{
  "name": "@butlerx/dotfiles",
  "private": true,
  "description": "Toolchain for the shared eslint and prettier configs. ESLint resolves a config's plugins relative to the config file, so they have to live here rather than in a global install.",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import-x": "^4.17.1",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "^3.9.6",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.70.0"
  }
}
